#include <iostream>
using namespace std;
int main() {
    // your code goes here
 
    int n;
    cin>>n;
    
    int i;
    
    while(n--){
        int a[5];
         int count=0;
        for(i=0;i<5;i++)
        {
             cin>>a[i];
              if(a[i]==1)
            {
                  count++;
            }
        }
       
 
        if(count==0)
        {
            cout<<"Beginner\n";
        }
        if( count==1){
            cout<<"Junior Developer\n";
        }
        if(count==2)
        {
            cout<<"Middle Developer\n";
        }
        if(count==3)
        {
            cout<<"Senior Developer\n";
        }
        if(count==4)
        {
            cout<<"Hacker\n";
        } 
        if(count==5){
            cout<<"Jeff Dean\n";
            
        }
    }
    return 0;
}
